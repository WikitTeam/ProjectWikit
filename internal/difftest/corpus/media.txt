# Requests aimed at the media host. One run carries one Host, so these cannot
# live in default.txt:
#
#   PWIKIT_DIFF_HOST=media.localhost PWIKIT_DIFF_CORPUS=corpus/media.txt go test ./internal/difftest -run TestCorpusDiff -v
#
# The probe files come from internal/media/testdata/oracle_seed.py.

/local--files/nonexistent.png
/local--files/-/probe/empty

# whole-file responses: Django streams them, and its gzip layer puts
# Accept-Encoding into Vary. pwikit does not compress yet -- reason in PROGRESS.
!/local--files/-/probe/a.pdf
!HEAD /local--files/-/probe/a.pdf
!/local--files/-/probe/a.txt
!/local--files/-/probe/a.txt.gz
!/local--files/-/probe/a.bin

# the redirect back to the main host, the other direction of the rule
# default.txt exercises
/main
/-/login

# the three routes that read a page's own source. the code, html and css blocks
# they find are relinked onto probeoff:unratable by oracle_seed.py.
/local--code/probeoff:unratable/1
/local--code/probeoff:unratable/2
/local--code/probeoff:unratable/3
/local--code/probeoff:unratable/1?revNum=0
/local--code/probeoff:unratable/1?revNum=99
/local--code/probe:full/1
/local--code/nope/1
HEAD /local--code/probeoff:unratable/1
OPTIONS /local--code/probeoff:unratable/1

!/local--code/probeoff:unratable/x
# django never reaches the view here: an index that is not a number matches no
# route, so it answers with its own 404 page instead of the view's line.

/local--html/probeoff:unratable/deadbeef-x
/local--html/nope/deadbeef-x
# the hash of a block that is there changes with the seed, so the matching one
# is left to internal/localitem/testdata/oracle_localitem.py.

/local--theme/probecss:styled/style.css
/local--theme/probeoff:unratable/style.css
/local--theme/probeoff:unratable/style.css?includeParams=%7B%22probecolor%22%3A%22blue%22%7D
/local--theme/probeoff:unratable/style.css?revNum=0
/local--theme/probe:full/style.css
/local--theme/1/style.css
